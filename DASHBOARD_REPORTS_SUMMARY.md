# สรุปการปรับปรุงระบบ Dashboard และ Reports

## 📋 ภาพรวม
ปรับปรุงระบบ Dashboard และ Reports ให้ดึงข้อมูลจาก Firestore ตาม schema ที่กำหนด พร้อมสร้าง Backend Function สำหรับคำนวณข้อมูลสรุปอัตโนมัติ

---

## 🗂️ Firestore Schema ที่ใช้

### 1. **users** (คอลเลกชันหลัก)
เก็บข้อมูลผู้ใช้ทั้งหมด
```javascript
{
  uid, email, firstName, lastName, phone, profileImageURL, createdAt, role,
  subscription: {
    status: "active" | "inactive" | "expired",
    packageId: "free" | "monthly" | "yearly",
    startDate, endDate, autoRenew, paymentMethodId
  }
}
```

### 2. **users/{userId}/payments** (คอลเลกชันย่อย)
ประวัติการชำระเงิน
```javascript
{
  amount, date, status: "succeeded" | "failed", 
  packageId, invoiceURL
}
```

### 3. **reports** (คอลเลกชันหลัก)
ข้อมูลสรุปที่คำนวณไว้ล่วงหน้า

#### 📄 `reports/daily_summary`
```javascript
{
  totalRevenue: 12134821,
  newMembers: 20375,
  churnRate: 4.2,
  renewalRate: 85.0,
  totalMembers: 46789,
  packageDistribution: { free: 30, monthly: 25, yearly: 45 },
  revenueByPackage: { free: 0, monthly: 50000, yearly: 150000 },
  top10Content: [{ contentId, title, watchMinutes, followerCount }],
  lastUpdated: Timestamp
}
```

#### 📄 `reports/monthly_YYYY_MM`
```javascript
{
  year: 2025,
  month: 11,
  revenue: 1000000,
  newMembers: 1500,
  renewalRate: 85.0,
  churnRate: 4.2,
  lastUpdated: Timestamp
}
```

### 4. **content** (คอลเลกชันหลัก)
ข้อมูลการ์ตูน
```javascript
{
  title, description, type: "series" | "movie",
  thumbnailURL, heroImageURL, requiresSubscription,
  tags, isRecommended, isFeaturedHero,
  totalWatchMinutes, followerCount, episodeCount
}
```

### 5. **packages** (คอลเลกชันหลัก)
แพ็คเกจที่มีให้สมัคร
```javascript
{
  name: "รายเดือน",
  price: 159,
  billingCycle: "month" | "year",
  features: [],
  stripePriceId
}
```

---

## 🔧 ไฟล์ที่สร้างใหม่

(ไฟล์ `functions/calculate-reports.js` ได้ถูกลบออกแล้ว และจะถูกแทนที่ด้วย Cloud Function)

## 🔄 ไฟล์ที่แก้ไข

### 1. `js/dashboard.js`
**อัปเดตให้ดึงข้อมูลจาก Firestore**

#### การเปลี่ยนแปลง:
- ✅ เพิ่ม `loadDashboardData()` - ดึงจาก `reports/daily_summary`
- ✅ เพิ่ม `loadMonthlyRevenue()` - ดึง 12 เดือนย้อนหลัง
- ✅ เพิ่ม `formatCurrency()` - แสดงผลเป็น M/K
- ✅ เพิ่ม `formatNumber()` - แสดงผลด้วยคอมม่า
- ✅ อัปเดต `renderRevenueChart()` - ใช้ข้อมูลจริง
- ✅ อัปเดต `formatRevenueByPackage()` - คำนวณจากข้อมูล Firestore

#### ข้อมูลที่แสดง:
```javascript
{
  totalRevenue: จาก reports/daily_summary.totalRevenue
  newMembers: จาก reports/daily_summary.newMembers
  churnRate: จาก reports/daily_summary.churnRate
  packageDistribution: จาก reports/daily_summary.packageDistribution
  revenueByPackage: จาก reports/daily_summary.revenueByPackage
  monthlyRevenue: จาก reports/monthly_YYYY_MM (12 เดือน)
}
```

---

### 2. `dashboard.html`

ลบ Script Block ที่เคยใช้เริ่มต้น `reportsCalculator` ออกทั้งหมด

```html
<!-- ลบ Script Block นี้ออก -->
<script type="module">
  import reportsCalculator from './functions/calculate-reports.js';
  reportsCalculator.start(); // เริ่มการทำงานอัตโนมัติ
</script>
```

### 3. `reports.html`
**เปลี่ยน Script จาก dashboard.js → reports.js**

```html
<!-- เดิม -->
<script type="module" src="js/dashboard.js"></script>

<!-- ใหม่ -->
<script type="module" src="js/reports.js"></script>
```

---

## 📊 การทำงานของระบบ

### Backend Process (Auto-calculation)
```
┌─────────────────────────────────────────────────┐
│ 1. เปิดหน้า dashboard.html                      │
│    → Backend Function เริ่มทำงาน                │
└──────────────────┬──────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────┐
│ 2. ทุก 3 นาที - calculate()                    │
│    → Query users collection                     │
│    → Query payments subcollections              │
│    → คำนวณ KPI ทั้งหมด                          │
└──────────────────┬──────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────┐
│ 3. บันทึกลง Firestore                          │
│    → reports/daily_summary                      │
│    → reports/monthly_YYYY_MM (12 เดือน)         │
└─────────────────────────────────────────────────┘
```

### Frontend Process (Data Display)
```
┌─────────────────────────────────────────────────┐
│ Dashboard.js                                    │
│ 1. loadDashboardData()                          │
│    → อ่าน reports/daily_summary                 │
│    → อ่าน reports/monthly_* (12 เดือน)          │
└──────────────────┬──────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────┐
│ 2. updateStatCards()                            │
│    → แสดงรายได้รวม                              │
│    → แสดงสมาชิกใหม่                             │
│    → แสดง Churn Rate                            │
└──────────────────┬──────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────┐
│ 3. initCharts()                                 │
│    → วาดกราฟแนวโน้มรายได้ (12 เดือน)             │
│    → วาดกราฟ Pie Chart (Package Distribution)   │
│    → วาดกราฟแท่ง (Revenue by Package)           │
└─────────────────────────────────────────────────┘
```

```
┌─────────────────────────────────────────────────┐
│ Reports.js                                      │
│ 1. loadReportsData()                            │
│    → อ่าน reports/daily_summary                 │
│    → อ่าน reports/monthly_* (12 เดือน)          │
└──────────────────┬──────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────┐
│ 2. updateRateCards()                            │
│    → แสดง Renewal Rate                          │
│    → แสดง Churn Rate                            │
│    → คำนวณ Rate Change (เปรียบเทียบเดือนก่อน)   │
└──────────────────┬──────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────┐
│ 3. renderTrendChart()                           │
│    → วาดกราฟเปรียบเทียบ Renewal vs Churn (SVG)  │
│    → แสดงค่าเปอร์เซ็นต์แต่ละจุดข้อมูล           │
└─────────────────────────────────────────────────┘
```

---

## 🔒 Security & Permissions
- ✅ ตรวจสอบสิทธิ์ `role === 'admin'` ก่อนเข้าหน้า Dashboard/Reports
- ✅ Redirect ไป `login.html` ถ้าไม่ได้ล็อกอิน
- ✅ Redirect ไป `index.html` ถ้าไม่ใช่ admin

---

## 📈 ข้อมูลที่แสดงในแต่ละหน้า

### Dashboard (dashboard.html)
| ข้อมูล | แหล่งที่มา |
|--------|-----------|
| รายได้รวม | `reports/daily_summary.totalRevenue` |
| สมาชิกใหม่ | `reports/daily_summary.newMembers` |
| Churn Rate | `reports/daily_summary.churnRate` |
| แนวโน้มรายได้ (12 เดือน) | `reports/monthly_*` |
| Package Distribution | `reports/daily_summary.packageDistribution` |
| Revenue by Package | `reports/daily_summary.revenueByPackage` |

### Reports (reports.html)
| ข้อมูล | แหล่งที่มา |
|--------|-----------|
| Renewal Rate | `reports/daily_summary.renewalRate` |
| Churn Rate | `reports/daily_summary.churnRate` |
| Renewal Change | คำนวณจาก `reports/monthly_*` (เดือนล่าสุด vs เดือนก่อน) |
| Churn Change | คำนวณจาก `reports/monthly_*` (เดือนล่าสุด vs เดือนก่อน) |
| แนวโน้ม 12 เดือน | `reports/monthly_*` |

### Members (members.html)
| ข้อมูล | แหล่งที่มา |
|--------|-----------|
| รายชื่อสมาชิก | `users` collection (กรอง `role !== 'admin'`) |
| แพ็คเกจปัจจุบัน | `users.subscription.packageId` |
| สถานะ | `users.subscription.status` |
| ประวัติชำระเงิน | `users/{userId}/payments` |

---

## 🚀 วิธีใช้งาน

### 1. เริ่มต้นระบบ
```javascript
// Backend Function จะเริ่มอัตโนมัติเมื่อเปิด dashboard.html
// ไม่ต้องทำอะไรเพิ่มเติม
```

### 2. หยุดการทำงาน (ถ้าต้องการ)
```javascript
// เปิด Console ในหน้า dashboard.html
reportsCalculator.stop();
```

### 3. คำนวณข้อมูลใหม่ทันที
```javascript
// เปิด Console ในหน้า dashboard.html
reportsCalculator.calculate();
```

### 4. ตรวจสอบข้อมูลใน Firestore
- เข้า Firebase Console → Firestore Database
- ดูคอลเลกชัน `reports`
- ตรวจสอบ `daily_summary` และ `monthly_*`

---

## ⚙️ การตั้งค่า (Configuration)

### เปลี่ยนความถี่การคำนวณ
แก้ไขไฟล์ `functions/calculate-reports.js`:
```javascript
// จากทุก 3 นาที (180,000 ms)
this.intervalId = setInterval(() => {
  this.calculate();
}, 180000);

// เป็นทุก 5 นาที (300,000 ms)
this.intervalId = setInterval(() => {
  this.calculate();
}, 300000);
```

---

## 🐛 การแก้ไขปัญหา (Troubleshooting)

### ข้อมูลไม่แสดง
1. ตรวจสอบ Console → ดูว่ามี error หรือไม่
2. ตรวจสอบ Firestore Rules → ต้องอนุญาตให้อ่าน/เขียน `reports`
3. ตรวจสอบว่า Cloud Function ที่จะสร้างขึ้นใหม่ทำงานถูกต้องหรือไม่

### Backend Function ไม่ทำงาน
(ส่วนนี้ไม่เกี่ยวข้องแล้ว เนื่องจาก `calculate-reports.js` ถูกลบออก)

### กราฟไม่แสดง
1. ตรวจสอบว่ามีข้อมูลใน `reports/monthly_*` หรือไม่
2. ตรวจสอบ CSS - element อาจถูกซ่อน
3. ตรวจสอบ Console → ดู error จาก rendering

---

## 📝 หมายเหตุ

1. **ข้อมูล Seed**: ถ้ายังไม่มีข้อมูลใน Firestore ระบบจะใช้ค่า default (0)
2. **Performance**: การคำนวณจะย้ายไปทำบน Cloud Functions เพื่อประสิทธิภาพและความปลอดภัย
3. **Real-time**: ข้อมูลจะถูกคำนวณเป็นรอบ (เช่น วันละครั้ง) ไม่ใช่ real-time จากฝั่ง client อีกต่อไป
4. **Billing**: การใช้ Cloud Function จะช่วยลดจำนวนการ read/write จาก client โดยตรง ทำให้ควบคุมค่าใช้จ่ายได้ดีขึ้น

---

## ✅ สรุป

ระบบได้รับการปรับปรุงให้:
- ✅ ดึงข้อมูลจาก Firestore ตาม schema ที่กำหนด
- ✅ มี Backend Function คำนวณข้อมูลอัตโนมัติ
- ✅ แสดงผลข้อมูลแบบ Real-time (3 นาที interval)
- ✅ รองรับการ scale ในอนาคต
- ✅ มีการตรวจสอบสิทธิ์และความปลอดภัย

**Dashboard** และ **Reports** พร้อมใช้งาน! 🎉
